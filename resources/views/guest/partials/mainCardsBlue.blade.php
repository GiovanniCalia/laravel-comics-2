{{--<div class="second-main">
        <div class="container">
            <ul class="ul-images">
                @foreach ($dc as $comic)
                    <li class="li-inline-block">
                        <a href="#!">
                           <img src="{{ $comic['image'] }}" alt="">
                           <div>{{ $comic['text'] }}</div>
                        </a>
                    </li>
                @endforeach
            </ul>
        </div>
    </div>--}}